# SIMULATED version of PR-01/simple_booking_ignacio.yaml: an LLM plays the caller, SPEAKING, and adapts to what the
# bot says, as the platform's caller does. The `# submit:` check is the hard assertion.
# submit: BOOK P00005 PR10 sur orthopaedic_review cigna
name: sim_simple_booking_ignacio
user:
  modality: audio
  speech:
    service: kokoro
    voice: af_heart
simulator:
  factory: eval_judge.helmcode_judge
judge:
  modality: audio
  transcription:
    service: moonshine
  eval:
    factory: eval_judge.helmcode_judge
persona: |
  Ignacio Vázquez Moreno. Has no ID at hand; phone number 731 169 716.
  A patient phoning Clínica Arenal, speaking English. Polite, brief, answers one question at a time and repeats or spells anything the receptionist asks for. Knows nothing about the clinic's rules. Accepts or declines only an appointment that was actually offered with a doctor, site, day and time.
goal: "Book the first orthopaedics appointment available, accept it, then end the call."
success: "the bot identified the caller by phone, offered one orthopaedics appointment and booked it after a yes"
max_turns: 12
