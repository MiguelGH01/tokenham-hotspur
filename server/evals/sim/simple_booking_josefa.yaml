# SIMULATED version of PR-01/simple_booking_josefa.yaml: an LLM plays the caller, SPEAKING, and adapts to what the
# bot says, as the platform's caller does. The `# submit:` check is the hard assertion.
# submit: BOOK P00001 * * review mapfre
name: sim_simple_booking_josefa
user:
  modality: audio
  speech:
    service: kokoro
    voice: af_heart
simulator:
  factory: eval_judge.helmcode_judge
judge:
  modality: audio
  transcription:
    service: moonshine
  eval:
    factory: eval_judge.helmcode_judge
persona: |
  Josefa Domínguez Navarro, DNI 48064716Y.
  A patient phoning Clínica Arenal, speaking English. Polite, brief, answers one question at a time and repeats or spells anything the receptionist asks for. Knows nothing about the clinic's rules. Accepts or declines only an appointment that was actually offered with a doctor, site, day and time.
goal: "Book the earliest general practitioner appointment, accept it, then end the call."
success: "the bot offered one specific GP appointment and booked it only after the caller said yes"
max_turns: 12
